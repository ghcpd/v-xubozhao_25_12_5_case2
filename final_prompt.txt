Given an integer array `nums`, a sliding window of size `k` moves from the leftmost to the rightmost position of the array. You can only see `k` numbers within the sliding window. The window moves one position to the right each time. Return the maximum value in the sliding window. Input: `nums = [1,3,-1,-3,5,3,6,7]`, `k = 3`